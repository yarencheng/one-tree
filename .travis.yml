dist: trusty
sudo: true

jobs:
  include:
    - stage: build
      script: docker run -it --rm -w /src -v $PWD/go-src:/src -v /go:/go golang go build -v ./...
    - stage: test
      script: docker run -it --rm -w /src -v $PWD/go-src:/src -v /go:/go golang go test -v ./...